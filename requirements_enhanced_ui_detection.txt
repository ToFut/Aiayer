# Enhanced UI Detection System Requirements
# Core dependencies for the better approaches integrated

# Base dependencies
Pillow>=8.0.0
numpy>=1.21.0
aiohttp>=3.8.0
asyncio-throttle>=1.0.0

# Machine Learning dependencies
scikit-learn>=1.0.0
joblib>=1.0.0

# OCR dependencies
easyocr>=1.6.0
pytesseract>=0.3.8

# NLP dependencies
spacy>=3.4.0
# Run: python -m spacy download en_core_web_sm

# Browser automation dependencies
selenium>=4.0.0
webdriver-manager>=3.8.0

# Platform-specific dependencies
# macOS
pyobjc-framework-Cocoa>=8.0; sys_platform == "darwin"
pyobjc-framework-Quartz>=8.0; sys_platform == "darwin"
pyobjc-framework-ApplicationServices>=8.0; sys_platform == "darwin"

# Windows
pywin32>=227; sys_platform == "win32"

# Linux
python-xlib>=0.31; sys_platform == "linux"
pyatspi>=2.38.0; sys_platform == "linux"

# Cross-platform utilities
pyautogui>=0.9.53
pygetwindow>=0.0.9
psutil>=5.8.0

# WebSocket for CDP
websocket-client>=1.3.0
requests>=2.28.0

# Development and testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Optional GPU acceleration for ML
# torch>=1.12.0
# tensorflow>=2.9.0

# Optional advanced OCR
# paddlepaddle>=2.3.0
# paddleocr>=2.6.0

# Installation instructions:
# 1. pip install -r requirements_enhanced_ui_detection.txt
# 2. python -m spacy download en_core_web_sm
# 3. For Chrome automation, install ChromeDriver or use webdriver-manager
# 4. For GPU acceleration, uncomment and install torch or tensorflow