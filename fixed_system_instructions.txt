If you need to manually start Tauri, run these commands:
cd /Users/segevbin/Desktop/SensAI/Aiayer/overlay
npm install
npm run tauri dev

To stop the backend system:
pkill -f 'python.*fixed_bridge' && pkill -f 'python.*fixed_process' && pkill -f 'python.*memory_system' && pkill -f 'python.*llm_service'

