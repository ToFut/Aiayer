# Core requirements
Flask==2.0.1
Werkzeug==2.3.7
PyYAML==5.4.1
requests==2.26.0
flask-sock==0.6.0  # WebSocket support
flask-cors==3.0.10

# Sensor modules
mss==6.1.0  # Screen capture
pytesseract==0.3.8  # OCR for screen text
Pillow==8.3.1  # Image processing
watchdog==2.1.3  # File system monitoring
psutil==5.8.0  # Process monitoring

# Optional: Windows support
pywin32==306; sys_platform == "win32"  # Windows-specific APIs

# LLM and processing
numpy==1.21.2
regex==2023.8.8
markdown==3.4.4

# Testing and development
pytest==7.4.0
pytest-mock==3.11.1

# Optional: Alternative model support if not using Ollama
# ctransformers==0.2.27  # For Windows support without Ollama
# transformers==4.34.0  # More advanced model loading options
# torch==2.0.1  # Required for some transformers models

python-dotenv==0.19.0
opencv-python==4.5.3.56